# Core
fastapi>=0.111.0
uvicorn[standard]>=0.30.0
pydantic>=2.7.0
pydantic-settings>=2.3.0

# Machine Learning
scikit-learn>=1.5.0
numpy>=1.26.0
pandas>=2.2.0
joblib>=1.4.0

# Database (will be needed once the DB layer is wired)
sqlalchemy>=2.0.0
aiosqlite>=0.20.0  # async SQLite for development

# RAG / LLM (install when building the RAG layer)
# langchain>=0.2.0
# openai>=1.30.0
# faiss-cpu>=1.8.0

# Testing
pytest>=8.2.0
pytest-asyncio>=0.23.0
httpx>=0.27.0  # required by TestClient in FastAPI

# Utilities
python-dotenv>=1.0.0
